// Playwright global setup — runs once before all E2E tests
// Use this to start/stop the dev server or seed test data

export default async function globalSetup() {
  // placeholder — wire up dev server or test DB here
}
