# API Reference

Welcome to the BuildSync API Reference. This page will be updated as new endpoints and features are added.

## Authentication
- POST /api/auth — User login and registration (see code for details)

## Stripe
- POST /api/checkout — Start a Stripe checkout session

## Docs
- GET /api/docs/[slug] — Fetch documentation content by slug

---

_This page will be expanded automatically as new API endpoints are added to the codebase._
