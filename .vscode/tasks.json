{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run tests",
			"type": "shell",
			"command": "npm test",
			"isBackground": false,
			"group": "test"
		},
		{
			"label": "Build and check Next.js app",
			"type": "shell",
			"command": "npm run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test",
			"isBackground": false,
			"problemMatcher": [
				"$tsc",
				"$eslint-stylish"
			],
			"group": "build"
		},
		{
			"label": "Build and check Next.js app",
			"type": "shell",
			"command": "npm run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test",
			"isBackground": false,
			"group": "build"
		},
		{
			"label": "Build and check Next.js app",
			"type": "shell",
			"command": "npm run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm test",
			"isBackground": false,
			"group": "build"
		}
	]
}